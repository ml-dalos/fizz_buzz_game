name: Testing
on: workflow_dispatch
jobs:
  runs-on: ubuntu-latest
  steps:
    - name: Get code
      uses: actions/checkout@v3
    - name: Install dependencies
      run: bundle install
    - name: Run tests
      run: rspec spec/
