#!/usr/bin/env ruby

require_relative "../lib/fizz_buzz_game.rb"

Signal.trap("INT") do
  puts "\nThanks for playing!"
  exit
end

puts "Use ^C to exit the game"

game = FizzBuzzGame.new
loop { game.run }
